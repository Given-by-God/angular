import { MakeTablePipe } from './make-table.pipe';

describe('MakeTablePipe', () => {
  it('create an instance', () => {
    const pipe = new MakeTablePipe();
    expect(pipe).toBeTruthy();
  });
});
